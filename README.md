# AI based Minesweeper

Uses heuristics components to determine safe moves, and probabilty to select when no safe moves are possible

Instructions to run:
```shell: 
pip install pygame
python runner.py
```